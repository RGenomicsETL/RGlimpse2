% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase-bam.R
\name{rglimpse2_phase_bam}
\alias{rglimpse2_phase_bam}
\title{Phase and impute one GLIMPSE2 chunk directly from one BAM or CRAM}
\usage{
rglimpse2_phase_bam(
  input_bam,
  input_index,
  reference_bin,
  output_bcf,
  executable,
  seed,
  sample_name = character(),
  sample_ploidy = integer(),
  reference_fasta = character(),
  reference_fasta_index = character(),
  mapq = 10L,
  baseq = 10L,
  max_depth = 40L,
  call_indels = FALSE,
  keep_orphan_reads = FALSE,
  ignore_orientation = FALSE,
  check_proper_pairing = FALSE,
  keep_failed_qc = FALSE,
  keep_duplicates = FALSE,
  burnin = 5L,
  main = 15L,
  ne = 100000L,
  pbwt_depth = 12L,
  pbwt_modulo_cm = 0.1,
  k_init = 1000L,
  k_pbwt = 2000L,
  log = character()
)
}
\arguments{
\item{input_bam}{Absolute \code{.bam} or \code{.cram} alignment path.}

\item{input_index}{Absolute BAM/CRAM index path. BAM accepts adjacent \code{.bai}
or \code{.csi} naming; CRAM accepts adjacent \code{.crai} naming.}

\item{reference_bin}{Absolute GLIMPSE2 binary reference path.}

\item{output_bcf}{Absolute \code{.bcf} output path.}

\item{executable}{Absolute selected \code{GLIMPSE2_phase} path.}

\item{seed}{Non-negative random seed.}

\item{sample_name}{Empty or one output sample name. When omitted, GLIMPSE2
derives the name from the alignment filename.}

\item{sample_ploidy}{Empty or one integer, \code{1L} or \code{2L}. If supplied without
\code{sample_name}, the alignment filename stem is used for the temporary
sample/ploidy table passed to GLIMPSE2.}

\item{reference_fasta}{Empty or one absolute faidx-indexed reference FASTA.
Required for CRAM input.}

\item{reference_fasta_index}{Empty or one absolute FASTA index path.
Required with \code{reference_fasta} and must be \code{paste0(reference_fasta, ".fai")}, which is the path HTSlib will discover.}

\item{mapq}{Minimum read mapping quality.}

\item{baseq}{Minimum base quality.}

\item{max_depth}{Maximum reads retained at one site before downsampling.}

\item{call_indels}{Whether to compute genotype likelihoods at reference
indels. The default leaves indel likelihoods flat while retaining the
reference haplotype scaffold.}

\item{keep_orphan_reads}{Whether to keep paired reads whose mate is unmapped.}

\item{ignore_orientation}{Whether to ignore mate-pair orientation.}

\item{check_proper_pairing}{Whether to discard reads not marked as properly
paired.}

\item{keep_failed_qc}{Whether to keep reads marked as failed sequencing QC.}

\item{keep_duplicates}{Whether to keep reads marked as duplicates.}

\item{burnin}{Number of burn-in iterations.}

\item{main}{Number of main iterations, at most 15.}

\item{ne}{Positive effective diploid population size.}

\item{pbwt_depth}{Positive PBWT neighbour depth.}

\item{pbwt_modulo_cm}{Positive PBWT selection spacing in cM.}

\item{k_init, k_pbwt}{Positive conditioning-state limits.}

\item{log}{Empty or one absolute log output path.}
}
\value{
\code{RGlimpse2RunResult} or an \code{RGlimpse2ErrorValue}.
}
\description{
Invokes \code{GLIMPSE2_phase} once against a binary reference chunk and one
coordinate-sorted, indexed alignment. The alignment index is an explicit
input and must use a filename that HTSlib's \code{sam_index_load()} will discover
beside the alignment. RGlimpse2 fixes the internal phase thread count at one
so a supplied seed is not coupled to worker scheduling; callers should
parallelize independent chunks.
}
