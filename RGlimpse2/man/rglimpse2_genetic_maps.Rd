% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genetic-maps.R
\name{rglimpse2_genetic_maps}
\alias{rglimpse2_genetic_maps}
\alias{rglimpse2_genetic_map}
\title{Resolve packaged GRCh37 or GRCh38 genetic maps}
\usage{
rglimpse2_genetic_maps()

rglimpse2_genetic_map(assembly = "GRCh38", chromosome, region = character())
}
\arguments{
\item{assembly}{Reference assembly, either \code{"GRCh37"} or \code{"GRCh38"}.}

\item{chromosome}{One autosome named \code{"1"} through \code{"22"}, \code{"X"}, \code{"Y"}, or
\code{"MT"}. A leading \code{"chr"} is accepted. Mitochondrial aliases \code{"M"},
\code{"MT"}, \code{"chrM"}, and \code{"chrMT"} all resolve to the same map.}

\item{region}{Empty to select the chromosome default, or one of \code{"full"},
\code{"nonpar"}, \code{"par1"}, or \code{"par2"}. The default is \code{"nonpar"} for X and
Y and \code{"full"} otherwise.}
}
\value{
\code{rglimpse2_genetic_map()} returns one absolute file path.
\code{rglimpse2_genetic_maps()} returns a data frame describing every map and
its absolute installed path.
}
\description{
\code{rglimpse2_genetic_map()} resolves one absolute installed map path.
\code{rglimpse2_genetic_maps()} returns the complete packaged inventory. The
autosomal and chromosome X files are byte-identical copies from the pinned
GLIMPSE source tree. The Y non-PAR and mitochondrial files are derived
two-anchor, zero-recombination coordinate maps, not empirical maps.
}
\examples{
rglimpse2_genetic_map("GRCh38", "22")
rglimpse2_genetic_map("GRCh37", "Y")
rglimpse2_genetic_map("GRCh38", "Y")
rglimpse2_genetic_map("GRCh37", "chrM")
rglimpse2_genetic_map("GRCh38", "MT")
head(rglimpse2_genetic_maps())
}
